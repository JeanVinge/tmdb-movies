excluded:
  - Pods
  - vendor

disabled_rules:

function_body_length:
  - 30

cyclomatic_complexity:
  - 7

identifier_name:
  min_length: 1
  max_length:
    warning: 50
    error: 100
  excluded:
    - id
    - URL
